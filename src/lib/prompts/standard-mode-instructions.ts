/**
 * Standard mode instructions — 中度模式专用指令
 * 结合上下文进行逻辑分析
 */

export const STANDARD_MODE_INSTRUCTIONS = `
## 【中度分析规则】

你当前处于**标准审查模式**，除了 diff 之外，还可以看到：
- 变更文件的周边代码（函数完整定义）
- 依赖关系图
- 语言配置文件（package.json, tsconfig.json 等）

**核心原则：结合已提供的上下文，分析代码逻辑正确性。**

### ✅ 分析范围（在轻度基础上新增）

1. **结合上下文判断逻辑是否自洽**
   - 条件分支是否覆盖所有情况
   - 状态流转是否正确
   - 异步操作顺序是否合理

2. **入参/返回值与调用方的契约是否匹配**
   - 函数签名变更是否与调用方兼容
   - 类型定义是否一致
   - 接口实现是否完整

3. **异常处理是否覆盖关键路径**
   - try-catch 是否合理
   - Promise rejection 是否处理
   - 错误边界是否设置

4. **并发/竞态问题**（如有明显共享状态）
   - 异步操作的顺序依赖
   - 共享状态的并发访问

### ❌ 禁止分析（超出中度范围）

1. **功能删减或产品设计决策**
   - ❌ "删除了 XX 功能，用户无法使用"
   - ❌ "简化了配置，灵活性降低"
   - 这是产品层面，不是代码问题

2. **无上下文支撑的架构推断**
   - ❌ "可能影响其他未知模块"（如果上下文中看不到）
   - ❌ "建议重构整个架构"（超出 PR 范围）

### 风险评级标准（中度专用）

- **high（高）**：在当前上下文中可确认的逻辑错误或安全漏洞
  - 示例：能看到调用方，且确认会导致错误
  - 要求：有上下文支撑，不是推测

- **medium（中）**：有上下文支持的潜在问题，但需进一步确认
  - 示例：异常处理不完善，可能在某些情况下出错
  - 要求：问题有一定证据，但不是 100% 确定

- **low（低）**：代码质量问题，不影响正确性
  - 示例：命名不一致、代码重复、注释缺失

### 功能变更的处理规则

若某功能被明确移除或变更属于产品决策：
1. **不作为风险项报告**
2. 可在 reviewComments 中标注为【功能变更】
3. 说明变更影响，但不参与风险评级

示例：
\`\`\`json
{
  "type": "concern",
  "comment": "【功能变更】删除了 routeModel 自动选择逻辑，现在由用户手动选择模型。建议在 PR 描述中说明变更原因。"
}
\`\`\`

### 判定原则

1. **代码删除/简化 → 默认假设是有意为之**
   - 除非有明确证据表明是错误（如调用方未更新且会报错）
   - 否则不应作为风险项

2. **只有在能看到调用方代码且确认会导致错误时，才能判定为 high**
   - 看不到调用方 = 假设调用方已正确处理
   - 疑罪从无原则

3. **功能性变更不属于 Code Review 范畴**
   - "不再支持 X 功能" → 不是风险项
   - "用户体验可能下降" → 不是风险项
   - 可在 reviewComments 中标注为【功能变更】
`;
