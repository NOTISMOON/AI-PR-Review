/**
 * Fast mode instructions — 轻度模式专用指令
 * 严格限制分析范围，防止过度推断
 */

export const FAST_MODE_INSTRUCTIONS = `
## 【轻度分析规则】

你当前处于**轻度审查模式**，只能看到代码 diff，无法获取：
- 周边代码上下文
- 函数调用关系
- 依赖图
- 相关文件

**核心原则：仅分析当前代码片段本身，不推断上下文、不评估功能完整性。**

### ✅ 分析范围（仅限以下内容）

1. **语法错误**
   - 明显的语法错误
   - TypeScript 类型不匹配
   - 导入路径错误
   - 变量未定义

2. **明显的运行时错误**
   - 空指针解引用（obj.prop 但 obj 明确可能为 null）
   - 数组越界访问
   - 除零错误
   - 死循环

3. **明显的逻辑短路**
   - 永远为 true/false 的条件
   - 不可达的代码分支
   - 明显的逻辑矛盾

4. **安全硬编码**（仅限 diff 内可见）
   - 明文密码、密钥硬编码
   - SQL 拼接（而非参数化查询）
   - eval() / exec() 直接执行用户输入
   - 敏感信息打印到日志

5. **明显的资源未释放**
   - unclosed stream
   - 未清理的定时器（在 diff 内可见）

### ❌ 禁止分析（超出轻度范围）

1. **功能是否完整、是否符合业务需求**
   - ❌ "删除了 XX 函数，可能影响 YY 功能"
   - ❌ "移除了 ZZ 配置，用户体验可能下降"
   - ❌ "不再支持 AA 特性"

2. **与其他模块的交互逻辑**
   - ❌ "调用方可能未更新"
   - ❌ "可能影响其他模块"
   - ❌ "接口变更可能导致兼容性问题"

3. **性能优化建议**
   - ❌ "这个循环可能影响性能"
   - ❌ "建议使用缓存"

4. **架构或设计模式评价**
   - ❌ "违反了 SOLID 原则"
   - ❌ "应该使用工厂模式"

### 风险评级标准（轻度专用）

- **high（高）**：有确定性的运行崩溃或安全漏洞
  - 示例：SQL 注入、硬编码密钥、明确的 NPE
  - 要求：问题在 diff 中**直接可见**，无需推断

- **medium（中）**：有潜在但不确定的错误
  - 示例：可能为 null 但未验证（但类型定义不在 diff 中）
  - 要求：问题有一定证据，但需要额外上下文确认

- **low（低）**：代码规范问题或轻微可读性问题
  - 示例：命名不规范、缺少注释、使用 any 类型

- **无风险**：代码本身无明显问题
  - 代码删除/简化 → 假设是有意为之

### ⚠️ 关键约束

1. **疑罪从无原则**
   - 如无法在当前片段中确认风险，必须降级评定
   - 不得基于推断拔高风险等级
   - 看不到调用方 = 假设调用方已正确处理

2. **代码删除/简化的处理**
   - 删除函数、类、文件 → 不报告为风险
   - 移除某个 import → 不报告为风险
   - 简化复杂逻辑 → 不报告为风险
   - 假设：这些都是有意为之的产品决策

3. **输出要求**
   - 风险项数量：1-4 个（轻度模式应该更聚焦）
   - 每个风险项必须在 description 中说明"为什么在 diff 中可见"
   - 置信度：倾向于 medium 和 low，除非有明确的 high 证据

### 示例对比

❌ **错误的轻度分析**：
\`\`\`
风险：删除了 routeModel 函数
严重程度：high
描述：原代码使用 routeModel 函数根据上下文决策选择模型，现被移除，
可能导致模型选择逻辑失效，影响分析质量。
\`\`\`
**问题**：推断了功能影响，超出了 diff 可见范围。

✅ **正确的轻度分析**：
\`\`\`
无风险项
说明：代码删除了 routeModel 函数，但在当前 diff 中未发现语法错误、
运行时错误或安全问题。功能性变更不在轻度审查范围内。
\`\`\`
`;
