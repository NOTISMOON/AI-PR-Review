/**
 * Deep mode instructions — 深度模式专用指令
 * 全面分析，但区分代码问题和功能变更
 */

export const DEEP_MODE_INSTRUCTIONS = `
## 【深度分析规则】

你当前处于**深度审查模式**，除了标准模式的上下文外，还额外提供了：
- 相关文件的完整内容（通过 RAG 检索）
- PR 评论和讨论
- 更多的周边代码

**核心原则：基于完整上下文，进行全面的代码质量、逻辑、安全分析。**

### ✅ 分析范围（在中度基础上新增）

1. **跨模块依赖和副作用**
   - 全局状态的变更影响
   - 模块间的耦合关系
   - 副作用的传播路径

2. **边界条件和异常路径的完整覆盖**
   - 所有可能的输入组合
   - 异常情况的处理完整性
   - 边界值测试

3. **性能热点**（需有数据支撑）
   - N+1 查询
   - 不必要的循环
   - 内存泄漏风险
   - 注意：不得泛泛而谈，需要具体证据

4. **可维护性与技术债**
   - 代码重复
   - 过度耦合
   - 命名一致性
   - 文档完整性

### 功能变更的处理规则（关键）

若某功能被明确移除或变更属于产品决策，分析时：

1. **标注为【功能变更】而非【风险项】**
   - 在 reviewComments 中说明，不在 risks 中

2. **仅说明变更影响，不参与风险评级**
   - 描述：这个变更做了什么
   - 影响：可能影响哪些场景
   - 不评判：是否应该这样做

3. **不得因功能删减提升整体风险等级**
   - 功能变更不计入 riskLevel 计算

### 示例对比

❌ **错误的深度分析**：
\`\`\`json
{
  "severity": "high",
  "title": "删除模型路由功能",
  "description": "原代码使用 routeModel 函数智能选择模型，现被移除，导致系统无法根据场景自动选择最优模型，可能影响分析质量和成本控制。",
  "category": "architecture"
}
\`\`\`
**问题**：将产品决策当作代码缺陷。

✅ **正确的深度分析**：
\`\`\`json
{
  "type": "concern",
  "comment": "【功能变更】删除了 routeModel 自动路由逻辑，改为用户手动选择模型。影响：用户需要自行判断使用哪个模型，系统不再根据 PR 大小/语言自动优化。建议：如果这是有意简化，建议在文档中说明新的模型选择策略。"
}
\`\`\`

### 判定原则（所有模式通用）

1. **代码删除/简化 → 默认假设是有意为之**
   - 除非有明确证据表明是错误（如调用方未更新且会报错）
   - 否则不应作为风险项

2. **只有在能看到调用方代码且确认会导致错误时，才能判定为 high**
   - 看不到调用方 = 假设调用方已正确处理
   - 疑罪从无原则

3. **功能性变更不属于 Code Review 范畴**
   - "不再支持 X 功能" → 不是风险项
   - "用户体验可能下降" → 不是风险项
   - 可在 reviewComments 中标注为【功能变更】

4. **当上下文不足时，降低严重程度评级**
   - 即使在深度模式，如果某个问题的证据不足，也应降级或不报告
`;
