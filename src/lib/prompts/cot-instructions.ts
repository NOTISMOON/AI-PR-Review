/**
 * Chain-of-Thought instructions — guides the model through a structured
 * reasoning process before producing the final JSON output.
 */

export const COT_INSTRUCTIONS = `
## 分析步骤（必须按顺序在思考中执行，但最终只输出 JSON）

### Step 1 — 变更概览
阅读 PR 元数据、PR 描述和文件列表。
- 这个 PR 的目的是什么？
- 变更范围有多大？
- 有没有 PR 描述中提到的已知问题或限制？

### Step 2 — 逐文件深度分析
对每个变更文件，按以下顺序检查：

a) **安全审查**：
   - 是否有未验证/未过滤的用户输入？
   - 是否有敏感数据暴露（密钥、密码、PII）？
   - 是否有不安全的 API 调用（eval, exec, system）？
   - 认证和授权是否正确实现？
   - 是否使用了不安全的加密算法（MD5, SHA1, DES）？

b) **逻辑审查**：
   - 边界条件是否正确处理（null, undefined, 空数组, 0）？
   - 异常处理是否完善？是否有空的 catch 块？
   - 是否存在竞态条件（异步操作顺序依赖）？
   - 类型转换是否安全？

c) **性能审查**：
   - 是否有 N+1 查询模式？
   - 循环中是否有不必要的计算或 IO？
   - 是否有内存泄漏风险（未清理的监听器、定时器、闭包引用）？
   - 是否适当使用了缓存？

d) **代码质量审查**：
   - 命名是否清晰且一致？
   - 函数是否过长（>50行）？
   - 是否有代码重复？
   - 是否有过度耦合的模块？

### Step 3 — 跨文件影响分析
- 变更的接口（函数签名、类、类型）是否与调用方兼容？
- 全局状态（配置、环境变量、数据库 schema）变更的影响范围？
- 新增的依赖是否合理？

### Step 4 — 风险汇总
- 列出所有发现的问题
- 按严重程度排序（critical > high > medium > low）
- 合并重复或高度相关的发现
- 为每个问题评估置信度
- 确认总体风险等级

### Step 5 — 输出
基于以上分析，输出结构化的 JSON 审查报告。

## 重要提醒
- 请在你的思考过程中完成以上分析步骤
- 但最终回复中**只包含** JSON 输出，不要包含思考过程
- 这是代码审查，不是代码赞美——诚实地指出问题
- 但也要识别做得好的地方，在 reviewComments 中给出正面评价
`;
